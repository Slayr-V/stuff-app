# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project was downgraded from SDK 57 to SDK 54 so it can run in the
published Expo Go app (App Store Expo Go was capped at SDK 54 at the time).
See package.json for the exact pinned versions — they come from SDK 54's
own bundledNativeModules.json, not guessed.
